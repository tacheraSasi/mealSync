# Server Configuration
PORT=5000

# Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mealsync_db

# Docker PostgreSQL Configuration (for local development)
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=mealsync_db
DB_PORT=5432

# Alternative: Neon Database URL (for production)
# DATABASE_URL=postgresql://username:password@host.neon.tech/database_name?sslmode=require

# JWT Configuration (if implementing authentication tokens in the future)
# JWT_SECRET=your_super_secret_jwt_key_here
# JWT_EXPIRES_IN=7d

# CORS Configuration (if needed for specific frontend URLs)
# FRONTEND_URL=http://localhost:5173

# Node Environment
NODE_ENV=development
